name: Security scan
on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  secret-scan:
    uses: Titan-Tim/.github/.github/workflows/secret-scan.yml@main
